#ifndef UCalculator
#define UCaclulator
//*************************************************

// includes
#include <iostream>
#include <string>
#include <map> 

using namespace std ;

namespace Calc {
	double	calculate(string &);
	string	delSpaces(string &);
	bool	isNum(string &);
	bool	isDigit(char);
}

//*************************************************
#endif
