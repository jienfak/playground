" Jien's vimrc.

" In many terminal emulators the mouse works just fine.
" By enabling it you can position the cursor,
" VIsually select and scroll with the mouse.
if( has('mouse') )
  set mouse=a
endif

" Vars.
set nowrap
set nu
set smartindent

" Folding.
set foldenable 
set foldmethod=indent

" Cursorline in GIU(It works too slow in CMD).
if has("gui_running")
	set cursorline
endif


" My colorscheme bad on 'slate'.
:colorscheme jien
:syntax on


" Syntax everywhere.
" :syn match Function /\(new\s\+\)\@<=\w\+/
" :syn match Operator /\(+\)\|\(-\)/

" NerdTree.
:nmap \nt :NERDTree . <enter>

"hi Folded	ctermbg=blue		ctermfg=black
"hi StatusLine	ctermbg=black		ctermfg=red
"hi StatusLineNC ctermbg=black		ctermfg=darkgrey
" hi ModeMsg	ctermbg=white		ctermfg=black
"hi LineNr	ctermbg=yellow		ctermfg=black
"hi VertSplit 	ctermbg=darkgrey 	ctermfg=darkgrey
" hi CursorLine	cterm=bold



